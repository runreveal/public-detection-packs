# Remove disabled property to start using the detection
disabled: true

name: new-aws-key-behavior
displayName: New AWS key behavior
description: >-
  The behavior of this AWS key changed and we've seen new eventNames in a short period of time.
file: new_aws_key_behavior.sql
categories:
  - cloudtrail
  - signal
mitreAttacks:
  - execution
severity: Medium
schedule: '*/15 * * * *'
parameters:
  window: '30'
  eventLimit: '3'
sourceTypes:
  - cloudtrail