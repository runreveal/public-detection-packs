# Remove disabled property to start using the detection
disabled: true

name: onepassword-unusual-client
displayName: OnePassword Unusual Client
file: unusual_client.sql
description: This detects OnePassword access from a client app that the user has not previously accessed OnePassword from.
categories:
  - onepassword
  - signal
severity: Low
mitreAttacks:
  - credential-access
sourceTypes:
  - onepassword
schedule: '*/15 * * * *'